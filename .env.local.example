# Neon Postgres — provisioned by Vercel Marketplace
# (run `vercel env pull .env.local` to populate)
DATABASE_URL=postgres://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require

# Optional: WhatsApp business number (defaults to demo)
NEXT_PUBLIC_WHATSAPP_NUMBER=919876543210
